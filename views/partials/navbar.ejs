<nav class="bg-white shadow-lg">
    <div class="max-w-7xl mx-auto px-4">
        <div class="flex justify-between h-16">
            <div class="flex items-center">
                <a href="/" class="flex items-center">
                    <img src="/images/campus-cart-logo.jpg" alt="Campus Cart Logo" class="h-16 w-auto mr-2">
                    <span class="text-xl font-semibold text-gray-800">Campus Cart</span>
                </a>
            </div>
            <div class="flex items-center space-x-4">
                <% if (typeof userData !== 'undefined' && userData) { %>
                    <span class="text-gray-700"><%= userData.email %></span>
                    <button onclick="handleLogout()" 
                        class="bg-red-600 text-white px-4 py-2 rounded-md hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-red-500 focus:ring-offset-2">
                        Logout
                    </button>
                <% } else { %>
                    <a href="/login" class="text-gray-700 hover:text-gray-900">Login</a>
                    <a href="/register" class="bg-indigo-600 text-white px-4 py-2 rounded-md hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                        Register
                    </a>
                <% } %>
            </div>
        </div>
    </div>
</nav> 